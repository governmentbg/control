<?php

declare(strict_types=1);

namespace tests;

// should NOT be abstract - declared as abstract to serve as a demo!
abstract class NewsTest extends CRUD
{
    protected $slug = 'news';
    protected $search = 'title';

    protected function getCreateData(): array
    {
        return [
            'lang' => '1',
            'org' => '1',
            'title' => $this->rand,
            'fordate' => date('d.m.Y'),
            'visible_beg' => date('d.m.Y H:i'),
            '_status' => 'draft',
            'content' => 'test'
        ];
    }
}
